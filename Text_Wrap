def wrap(string, max_width):
    ans = ""
    for i in range(0,len(string), max_width):
        modified = string[i:i+max_width] + "\n"
        ans += modified
    return ans

if __name__ == '__main__':
    string, max_width = input(), int(input())
    result = wrap(string, max_width)
    print(result)
